/* Generic IBM Spectrum LSF execution profile. Add the site queue/project with -c. */

executor {
    name = 'lsf'
    queueSize = 100
    submitRateLimit = '1 sec'
}

process {
    executor = 'lsf'
}
